# NEU-DET 数据集配置
# 用于模型训练、验证和 INT8 量化校准

path: ../datasets/NEU-DET  # 数据集根目录 (建议使用相对于项目根目录的路径或绝对路径)
train: images/train        # 训练集路径
val: images/val            # 验证集路径 (INT8 量化校准必需)

# 类别映射
names:
  0: crazing
  1: inclusion
  2: patches
  3: pitted_surface
  4: rolled-in_scale
  5: scratches
